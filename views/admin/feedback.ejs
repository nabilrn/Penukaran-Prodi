<%- include("../sidebar.ejs") %>
<div class="w-full pt-10 px-4 sm:px-6 md:px-8 lg:ps-72">
    <div class="container">
        <h1 class="text-2xl font-bold mb-6">Feedback</h1>
        <div class="feedback-container space-y-4">
            <% feedbacks.forEach(feedback => { %>
            <div class="flex justify-start">
                <div class="bg-green-100 rounded-lg p-4 shadow-md max-w-[90%] self-start relative">
                    <div class="before:content-[''] before:absolute before:top-3 before:left-[-10px] before:border-8 before:border-transparent before:border-r-blue-100">
                        <p class="text-gray-800"><%= feedback.pesan %></p>
                    </div>
                </div>
            </div>
            <% }); %>
        </div>
    </div>
</div>
</body>
</html>
